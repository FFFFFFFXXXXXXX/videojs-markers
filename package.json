{
  "name": "videojs-markers",
  "description": "video marker breaks plugin for video.js",
  "version": "1.1.0",
  "source": "./src/videojs-markers.ts",
  "targets": {
    "default": {
      "context": "browser"
    }
  },
  "scripts": {
    "lint": "tsc",
    "watch": "parcel watch",
    "build": "cp ./src/videojs.markers.css ./dist/ && parcel build"
  },
  "peerDependencies": {
    "video.js": "^8.10.0"
  },
  "devDependencies": {
    "@parcel/packager-ts": "^2.11.0",
    "@parcel/transformer-typescript-types": "^2.11.0",
    "parcel": "^2.11.0"
  }
}
